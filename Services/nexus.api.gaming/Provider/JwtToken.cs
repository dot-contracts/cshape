
using System;
using System.Collections.Generic;
using System.IdentityModel;
using System.IdentityModel.Tokens;
using System.Linq;
using System.Security.Claims;

namespace nexus.web.auth
{

    //public interface IJwtToken
    //{
    //    SecurityToken CreateToken(int expiration, params Claim[] claims);

    //    SecurityToken CreateToken(params Claim[] claims);

    //    bool IsValid(SecurityToken token);

    //    ClaimsPrincipal GetClaimsPrincipal(SecurityToken token);

    //    string GetClaimValue(SecurityToken token, string claimType);

    //    SecurityToken GetToken(string value);

    //    string GetTokenString(SecurityToken token);
    //}
    public class JwtToken
    {
    }
}
