<my:NxPanelBase
    x:Class="nexus.plugins.floor.DashBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:my="using:nexus.common.control"
    xmlns:ch="using:nexus.common.charts"
    
    mc:Ignorable="d"    d:DesignWidth="1200" d:DesignHeight="800">

    <Grid x:Name="MainLayout" Margin="0" >
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Section -->
        <Grid Grid.Row="0" ColumnSpacing="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <my:NxTabData  x:Name="Payouts"   Grid.Column="0" Height="300" PageSize="10" RowHeight="25"  />
            <ch:NxBarChart x:Name="BarChart" Grid.Column="1" Height="300" ChartTitle="15 Minute Turnover"  />
        </Grid>

        <!-- Bottom Section -->
        <Grid Grid.Row="1"  >
            
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Tabs -->
            <my:NxCtlStrip x:Name="ResultTabs" Grid.Row="0" StripType="Tab"  List="Paging;Player;Card;Audit" Margin="0" Padding="0" Height="32" />

            <!-- Results Grid -->
            <my:NxTabData x:Name="Results" Grid.Row="1" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Margin="0" Padding="0" PageSize="15" RowHeight="25" ShowMenu="True" />
        </Grid>
    </Grid>
</my:NxPanelBase>
