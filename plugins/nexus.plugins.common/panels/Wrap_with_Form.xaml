<ContentControl
    x:Class="nexus.plugins.common.WrapWithForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:nexus.plugins.common"
    xmlns:nx="using:nexus.common.control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    >

    <Grid x:Name="RootPanel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Toolbar row -->
            <RowDefinition Height="*"/>     <!-- Main content -->
        </Grid.RowDefinitions>

        <!-- Toolbar StackPanel -->
        <StackPanel x:Name="ButtonToolbar"
                Grid.Row="0"
                Orientation="Horizontal"
                Height="40"
                Margin="5">
        </StackPanel>

        <Grid  Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="FormColumn" Width="0" />
            </Grid.ColumnDefinitions>

            <!-- Main results grid -->
            <ScrollViewer>
                <WrapPanel x:Name="Wrapper" />
            </ScrollViewer>

            <!-- Editor panel with scroll support -->
            <Border
            Grid.Column="1"
            Background="#FFF"
            BorderBrush="#888"
            BorderThickness="1"
            Margin="4"
            Padding="8"
            Visibility="Collapsed"
            x:Name="FormPanel"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="Panel" />
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</ContentControl>